/**
 * Shared helpers and constants for inbound E2E tests.
 *
 * Parallel to outbound-helpers.ts — re-exports Push Chain contract
 * addresses and ABIs used by inbound (Route 1) test scenarios.
 */
export { COUNTER_ADDRESS_PAYABLE } from '../../src/lib/push-chain/helpers/addresses';
export { COUNTER_ABI_PAYABLE } from '../../src/lib/push-chain/helpers/abis';
